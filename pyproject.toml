[project]
name = "natlang-training"
version = "0.1.0"
description = "Training-data preparation and model fine-tuning tools for the natlang interpreter model"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
