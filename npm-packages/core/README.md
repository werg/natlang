# `@natlang/core`

Platform-neutral natlang reduction and model-agent machinery. Hosts supply an
`EvalEnvironment`; the core does not select Node, browser, sandbox, filesystem,
or process semantics. Most Node applications should install `@natlang/node`.
