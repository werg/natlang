# `@natlang/node`

Node.js host APIs for running natlang programs, building terminal applications, and managing native `.nlpkg` packages. Install `@natlang/cli` when you want the `natlang` executable.
