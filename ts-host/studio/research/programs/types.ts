export type State = { revision: number; head: string; question: string; notice: string; active_view: string; selected: string; receipts: string[] };
export type Event = { id: string; kind: string; value: string };
export type Entry = { path: string; id: string; kind: string };
export type Hit = { path: string; id: string; kind: string; offset: number; excerpt: string };
export type Edit = { path: string; kind: string; content: string };
export type Proposal = { purpose: string; edits: Edit[]; removes: string[]; checks: string[]; unresolved: string[]; reasoning: string };
export type CommitResult = { head: string; detail: string };
export type CandidateResult = { id: string; detail: string };
export type Branch = { id: string; parent: string; message: string; changed: number; active: boolean; kind: string };
export type Receipt = { id: string; status: string; value: string; detail: string };
export type View = { heading: string; summary: string; active_view: string; suggestions: string[] };
